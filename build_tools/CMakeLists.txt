CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT(HTool)


INCLUDE_DIRECTORIES(
	${CMAKE_CURRENT_BINARY_DIR})

#Add app that generates header files from cl-files
ADD_EXECUTABLE(TxtToH "TxtToH.cpp")
SET_TARGET_PROPERTIES(TxtToH PROPERTIES OUTPUT_NAME "TxtToH")
